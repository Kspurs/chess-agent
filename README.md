# Chess Agent

A conversational chess platform for web, desktop, and CLI. The agent coordinates
deterministic chess services through typed tools; it never treats model output as
authoritative game state.

## TODO

- [x] Complete the architecture decisions in `docs/architecture.md`.
- [x] Bootstrap the TypeScript workspace and package manager.
- [ ] Implement the web-first vertical slice in `docs/mvp.md`.
- [ ] Add CI for type-checking, tests, linting, and agent evaluations.
