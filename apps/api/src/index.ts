/** HTTP and WebSocket API entry point. */

// TODO: Select the server framework and expose authenticated session endpoints.
// TODO: Connect requests to the agent runtime and stream messages/events.
// TODO: Add rate limits, request IDs, structured errors, and audit logging.

